<ul class="categories">
  <% @channels.each do |channel| %>
    <li class="categories__list-item">
      <% active = channel[:title] == @channel ? 'active' : '' %>
      <%= link_to channel[:title], channel[:link], class: "categories__category #{active}" %>
    </li>
  <% end %>
</ul>
